<Window 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="WimWitch-Reloaded Update Available" 
    Height="250" Width="450" 
    WindowStartupLocation="CenterScreen"
    ResizeMode="NoResize">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" 
                   Text="WimWitch-Reloaded Update Available" 
                   FontSize="18" FontWeight="Bold" 
                   Margin="0,0,0,15"/>
        
        <TextBlock Grid.Row="1" 
                   Text="A new version of WimWitch-Reloaded (VersionNumber) is available." 
                   TextWrapping="Wrap"
                   Margin="0,0,0,10"/>
        
        <TextBlock Grid.Row="2" 
                   Text="Would you like to update now?" 
                   TextWrapping="Wrap"
                   Margin="0,0,0,10"/>
                   
        <TextBlock Grid.Row="3" 
                   Text="Note: The application will need to be restarted after the update is complete."
                   TextWrapping="Wrap"
                   Foreground="DarkOrange"
                   Margin="0,0,0,10"/>
        
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnYes" Content="Yes" Width="75" Height="25" Margin="0,0,10,0"/>
            <Button x:Name="btnNo" Content="No" Width="75" Height="25"/>
        </StackPanel>
    </Grid>
</Window>